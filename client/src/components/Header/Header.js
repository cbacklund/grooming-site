import React from "react";

const Header = props => (
    <div className="jumbotron jumbotronn-fluid">
        <h1 className="display-4 text-left">Groomers Anonymous</h1>

    
    </div>     
);

export default Header;